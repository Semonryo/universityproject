#pragma once

/*
* @brief Ќайти обратную матрицу
* @param s_ : размер матрицы
* @return ќбратна€ матрица
*/
unsigned long long revmat(unsigned int s_);

/*
* @brief решить систему уравнений по методу  рамера
* @param n_ : размер матрицы
*/
unsigned long long cramer(unsigned int n_);